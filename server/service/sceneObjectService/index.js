"use strict";

const { resolveSceneObjectDef } = require("./defs");
const { createRuntimeSceneObject } = require("./runtime");

module.exports = {
  createRuntimeSceneObject,
  resolveSceneObjectDef,
};
